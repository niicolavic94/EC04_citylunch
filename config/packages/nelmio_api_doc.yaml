nelmio_api_doc:
    documentation:
        info:
            title: EC04 CityLunch API
            description: API pour la gestion des produits, livreurs et sacs.
            version: 1.0.0
        components:
            securitySchemes:
                bearerAuth:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - bearerAuth: []
